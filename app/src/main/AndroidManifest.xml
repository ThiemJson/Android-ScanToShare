<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="teneocto.thiemjason.tlu_connect">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TLUConnect">
        <activity android:name=".support.Support"></activity>
        <activity android:name=".scanninghistory.ScanningHistory" />
        <activity android:name=".imageslider.ImageSliderTest" />
        <activity android:name=".profile.Profile" />
        <activity android:name=".register.RegisterSocialNetwork" />
        <activity android:name=".recycleview.SocialRecycleView" />
        <activity android:name=".notification.Notification" />
        <activity android:name=".home.Home" />
        <activity android:name=".loading.Launcher" />
        <activity android:name=".register.RegisterProfile" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>